# Jupyter Kernel

After creating and activating your project environment:

```bash
pip install -e .[dev]
python -m ipykernel install --user --name erddap-client --display-name "erddap-client"
```

Select the `erddap-client` kernel in Jupyter for notebooks in this repo.
